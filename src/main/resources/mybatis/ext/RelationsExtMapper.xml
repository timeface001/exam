<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fs.ntes.domain.ext.RelationsExtMapper">

    <update id="updateQuestion" parameterType="java.util.Map">
        /*DELETE FROM relations WHERE initial_id=#{paperId};*/
        INSERT INTO relations(initial_id,refer_id,refer_type) VALUES (#{paperId},#{paperId},2)
        <!--<foreach collection="questionIds" item="itemId" open="" close="" separator="," index="index">
            (#{paperId},#{paperId},2)
        </foreach>-->
        <!--INSERT INTO relations(initial_id,refer_id,refer_type) VALUES
        <foreach collection="questionIds" item="item" open="(" close=")" separator="," index="index">
            #{paperId},(SELECT point_id FROM relations WHERE id=#{item}),1
        </foreach>;
        INSERT INTO relations(initial_id,refer_id,refer_type) VALUES
        <foreach collection="questionIds" item="item" open="(" close=")" separator="," index="index">
            #{paperId},(SELECT item_id FROM relations WHERE id=#{item}),0
        </foreach>;-->
    </update>
</mapper>